export enum AppRoute {
  Login = '/login',
  Main = '/',
  Favorite = '/favorite',
  Property = '/offer',
}

export enum AuthorizationStatus {
  Auth = 'AUTH',
  NoAuth = 'NO_AUTH',
  Unknown = 'UNKNOWN',
}
